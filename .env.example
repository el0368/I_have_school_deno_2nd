# Copy this file to .env and fill in the values
# cp .env.example .env

# Required
SESSION_SECRET=change_me_to_a_long_random_string

# The URL your app runs on
APP_URL=http://localhost:5173

# The Rust math engine server (run via: cargo run from math_engine/)
MATH_ENGINE_URL=http://localhost:8080

# Optional: External SQL database (leave blank to use Deno KV)
DATABASE_URL=
